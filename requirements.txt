# Manhaj — Python dependencies for the ETL + future server-side jobs
# Install: pip install -r requirements.txt (inside a venv)

# Excel reading (existing parser)
openpyxl>=3.1,<4.0

# Supabase Python client (URL + service role key — no direct DB connection needed)
supabase>=2.0,<3.0

# Env-var loading from .env
python-dotenv>=1.0,<2.0

# HTTP client for Supabase Storage uploads (etl/upload_source_to_supabase.py)
requests>=2.31,<3.0

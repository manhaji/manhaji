# Sprint 1.6 — Wave A + A6 migration review

**Date:** 2026-07-24 · **Reviewer:** Manhaji PM (conductor, inline) · **PRs:** #14 (Wave A, `sprint-1.6-waveA-admin`→`sprint-1.6`), #13 (migration `021`, `sprint-1.6-A6-migration`→`sprint-1.6`). Both OPEN, based on `sprint-1.6` (not main). Read-only review.

## PR #14 — Admin Dashboard Wave A

| Item | Acceptance | Verdict |
|------|-----------|---------|
| **A1** role tabs | Teacher removed; Advisor "coming soon" + inert; Principal default | **PASS (code)** — `BreadcrumbLensBar` now takes `lenses` + `comingSoon` props, shared `Lens` type kept. Only consumer is the admin faculty page → low blast radius. |
| **A2** dept chips filter | Chip filters+expands breakdown; All resets; over-cap/contracts-due narrow; active selected | **PASS (code)** — chips generated from real depts via shared `computeDeptRows`, drive the breakdown. |
| **A3** breakdown bucketing | Dept row expands to its teachers on real data; collapse; counts correct | **PASS (code)** — rows are `aria-expanded` buttons revealing name·subject·sections·load·status from real grouping. |
| **A4** header sort | Headers cycle asc→desc→default w/ arrow; text+numeric; filters still work | **PASS (code)** — `aria-sort`, 3-state, filters preserved. Excel-style per-column dropdown deferred (fast-follow, as planned). |
| **A4b** real Sections/Contract cols | Real roster branch shows Sections + Contract from real data | **PASS (code)** — `TeacherWithLoad` extended `weekly_sections` (from `timetable_slots` via new `getTeacherSectionCounts`) + `has_contract` (from `teacher_contracts` presence). |

**Shared-package safety:** `teachers.ts` change is purely additive — two new always-populated fields, `getTeachersWithLoad(academicYearId)` signature unchanged; the other consumer (`apps/admin/app/reports/generate/[slug]/route.ts`) is unaffected. `BreadcrumbLensBar` prop change is backward-compatible and used only on the faculty page. **No live DB writes, no main push, no secrets, no unrelated files.** `turbo lint/build/test` (173/173) green locally; Vercel deploy green.

**Gap — needs a human click-through.** The build env had no Supabase/browser tooling, so interactions were verified by markup + types + lint, **not driven in a live browser**. Elias's Vercel-preview pass is the real end-to-end check (his login clears the preview wall).

### Preview click-through checklist (Elias) — Admin → Faculty
1. Role tabs: **Teacher** gone; **Student Advisor** shows a "coming soon"/"Soon" ribbon and does nothing when clicked; **Principal** is the default.
2. Department chips: click **Mathematics** → the Department Breakdown filters to Mathematics and that bucket opens; **All departments** resets; **Over capacity** / **Contracts due** narrow the list.
3. Department Breakdown: click a department row → it expands to the teachers inside it; click again → collapses.
4. Roster sort: click a column header (e.g. **Periods/wk**) → ascending; again → descending; again → back to default; arrow indicator shows; existing filter dropdowns still work.
5. Roster columns: **Sections** and **Contract** columns are present and populated (Contract shows On file / Not on file).

### Decision needed — "Contracts due" definition
Real teacher records carry **no contract-expiry date**, so the build defined **"Contracts due" = teachers with no current-year contract on file** (`has_contract === false`, derived from `teacher_contracts` presence). Honest real-data signal, but not literally "expiring/renewal-due." **Confirm this interim meaning or specify the metric you want** (e.g. once expiry dates exist in the data, switch to date-based).

## PR #13 — migration `021_job_applicant_notes` (drafted, NOT applied)
Well-formed and safe to sit as a draft: `CREATE TABLE IF NOT EXISTS` (additive, touches nothing existing); RLS **ON** with the house `school_id = tenant_id()` policy (identical to `job_applicants`/020 tables); reuses the `applicant_status` enum (no parallel enum); FKs `applicant_id`→`job_applicants` ON DELETE CASCADE, `created_by`→`school_admins` ON DELETE SET NULL; two CHECK constraints enforce "no empty row" and "status change ⇒ non-blank comment." Only `DROP` is a commented rollback line. **Applies only with Elias's explicit sign-off**, and only when A6 front-end is ready to use it.

## Bottom line
Wave A is code-complete and safe; it needs Elias's preview click-through + the "Contracts due" call. Migration 021 waits for approval. On approval of Wave A, proceed to Wave B (A5 contract popup) and A6 front-end.
